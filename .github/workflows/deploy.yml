name: 🚀 MLOps Deployment Pipeline

on:
  workflow_dispatch: # Запускать вручную

env:
  # Настройки GHCR
  GHCR_USERNAME: ${{ github.actor }}
  REPO_NAME: ${{ github.event.repository.name }}
  IMAGE_BACKEND: ghcr.io/${{ github.actor }}/${{ github.event.repository.name }}-backend
  IMAGE_FRONTEND: ghcr.io/${{ github.actor }}/${{ github.event.repository.name }}-frontend

jobs:
  # 1. СБОРКА И ПУБЛИКАЦИЯ ОБОИХ ОБРАЗОВ
  build_and_push_deployment:
    runs-on: ubuntu-latest
    permissions:
      contents: read # Чтобы читать код
      packages: write # Чтобы писать в GHCR (Container Registry)
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Сборка и пуш Backend
      - name: Build and Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.backend
          push: true
          tags: ${{ env.IMAGE_BACKEND }}:latest

      # Сборка и пуш Frontend
      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.frontend
          push: true
          tags: ${{ env.IMAGE_FRONTEND }}:latest

# 2. РАЗВЕРТЫВАНИЕ НА EC2 ЧЕРЕЗ SSM
  deploy_to_ec2:
    runs-on: ubuntu-latest
    needs: [build_and_push_deployment]
    
    env:
      AWS_REGION: ${{ secrets.AWS_REGION }}
      S3_BUCKET_NAME: ${{ secrets.S3_BUCKET_NAME }} # <-- Убедитесь, что этот секрет определен!
      DEPLOYMENT_INSTANCE_NAME: MNIST-anomaly-MLOps-Deployment-Server
      
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
          
      - name: Get Deployment Instance ID
        id: get_id
        run: |
          INSTANCE_ID=$(aws ec2 describe-instances \
            --filters "Name=tag:Name,Values=${{ env.DEPLOYMENT_INSTANCE_NAME }}" \
            --query "Reservations[].Instances[].InstanceId" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT

      - name: Deploy with Docker Compose via SSM
        env:
          INSTANCE_ID: ${{ steps.get_id.outputs.INSTANCE_ID }}
          # GITHUB_TOKEN нужен для логина в GHCR
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
        run: |
          if [ -z "$INSTANCE_ID" ]; then
            echo "Deployment instance not found!"
            exit 1
          fi
          
          # --- 1. Копируем docker-compose.yml в S3 (Используем env.S3_BUCKET_NAME)
          aws s3 cp docker-compose.yml s3://${{ env.S3_BUCKET_NAME }}/deployment/docker-compose.yml

          SSM_COMMAND="
          # 1. Настройка рабочей среды
          DEPLOY_DIR=/opt/mlops-deploy
          
          # Создаем директорию и передаем права ec2-user, чтобы избежать ошибки Errno 13
          sudo mkdir -p $DEPLOY_DIR
          sudo chown -R ec2-user:ec2-user $DEPLOY_DIR
          cd $DEPLOY_DIR
          
          # 2. Скачивание docker-compose из S3 (Используем env.S3_BUCKET_NAME)
          aws s3 cp s3://${{ env.S3_BUCKET_NAME }}/deployment/docker-compose.yml ./docker-compose.yml
          
          # 3. ЭКСПОРТ ПЕРЕМЕННЫХ для Docker Compose
          # GitHub Actions подставит значения secrets/env сюда, прежде чем отправить команду
          export AWS_REGION=${{ env.AWS_REGION }}
          export S3_BUCKET_NAME=${{ env.S3_BUCKET_NAME }}
          
          # 4. Аутентификация Docker в GHCR (Используем GH_TOKEN из env)
          echo ${{ env.GH_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # 5. Обновление образов и запуск
          docker compose pull
          docker compose up -d --remove-orphans
          "

          # Отправка команды SSM
          aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --comment "MLOps Deployment" \
            --parameters commands="$SSM_COMMAND" \
            --output text