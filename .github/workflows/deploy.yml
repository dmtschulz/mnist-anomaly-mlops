name: 🚀 MLOps Deployment Pipeline

on:
  workflow_dispatch: # Запускать вручную

env:
  # Настройки GHCR
  GHCR_USERNAME: ${{ github.actor }}
  REPO_NAME: ${{ github.event.repository.name }}
  IMAGE_BACKEND: ghcr.io/${{ github.actor }}/${{ github.event.repository.name }}-backend
  IMAGE_FRONTEND: ghcr.io/${{ github.actor }}/${{ github.event.repository.name }}-frontend
  
  # Настройки AWS Deployment
  AWS_REGION: ${{ secrets.AWS_REGION }}
  DEPLOYMENT_INSTANCE_NAME: mnist-anomaly-MLOps-Deployment-Server # Имя вашего нового инстанса
  

jobs:
  # 1. СБОРКА И ПУБЛИКАЦИЯ ОБОИХ ОБРАЗОВ
  build_and_push_deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Сборка и пуш Backend
      - name: Build and Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.backend
          push: true
          tags: ${{ env.IMAGE_BACKEND }}:latest

      # Сборка и пуш Frontend
      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.frontend
          push: true
          tags: ${{ env.IMAGE_FRONTEND }}:latest

  # 2. РАЗВЕРТЫВАНИЕ НА EC2 ЧЕРЕЗ SSM
  deploy_to_ec2:
    runs-on: ubuntu-latest
    needs: [build_and_push_deployment] # Запускаем только после успешной сборки
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4 # Нужно для docker-compose.yml

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
          
      - name: Get Deployment Instance ID
        id: get_id
        run: |
          INSTANCE_ID=$(aws ec2 describe-instances \
            --filters "Name=tag:Name,Values=${{ env.DEPLOYMENT_INSTANCE_NAME }}" \
            --query "Reservations[].Instances[].InstanceId" \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT

      - name: Deploy with Docker Compose via SSM
        env:
          INSTANCE_ID: ${{ steps.get_id.outputs.INSTANCE_ID }}
        # Отправляем команды на Deployment Server
        run: |
          if [ -z "$INSTANCE_ID" ]; then
            echo "Deployment instance not found!"
            exit 1
          fi

          # 1. Копируем docker-compose.yml на сервер
          aws s3 cp docker-compose.yml s3://${{ secrets.S3_BUCKET_NAME }}/deployment/docker-compose.yml

          SSM_COMMAND="
          # Скачивание docker-compose
          aws s3 cp s3://${{ secrets.S3_BUCKET_NAME }}/deployment/docker-compose.yml ./docker-compose.yml
          
          # Аутентификация Docker
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # Обновление образов и запуск
          docker compose pull
          docker compose up -d --remove-orphans
          "

          # Отправка команды SSM
          aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --comment "MLOps Deployment" \
            --parameters commands="$SSM_COMMAND" \
            --output text
          
          # Ожидание завершения (можно добавить aws ssm wait, но для deploy - необязательно)